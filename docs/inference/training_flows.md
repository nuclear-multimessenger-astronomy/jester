# Training normalizing flows

This documentation page helps users set up their own workflows for training normalizing flows. 

`jester` makes use of normalizing flows in evaluating the likelihoods for some 